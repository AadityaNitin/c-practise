#include<stdio.h>
int main(int argc, char const *argv[])
{
    char character;
    char a,e,i,o,u;
    printf("Enter the alphabet:\n");
    scanf("%c",&character);
    if (character == 'a' || character == 'e',i,o,u);
    {
        printf("Given alphabet is a vowel");
    }
    else{
        printf("Given alphabet is a consonant");
    }
    return 0;
}
